language: php

php:
  - 7.2

addons:
  ssh_known_hosts: soundbox.igotta.beer

cache:
  directories:
    - $HOME/.composer/cache
    - web/node_modules

before_install:
    - composer self-update
    - composer validate --no-check-publish

install:
  - export PATH="$HOME/.composer/vendor/bin:$PATH"
  - composer install
  - cd web/
  - npm install

script:
  - npm run build

before_deploy:
  - openssl aes-256-cbc -K $encrypted_5b0d89742976_key -iv $encrypted_5b0d89742976_iv -in deploy_rsa.enc -out /tmp/deploy_rsa -d
  - eval "$(ssh-agent -s)"
  - chmod 600 /tmp/deploy_rsa
  - ssh-add /tmp/deploy_rsa

deploy:
  provider: script
  skip_cleanup: true
  script: rsync -r --delete-after --quiet $TRAVIS_BUILD_DIR automate@soundbox.igotta.beer:/var/www/geekdpt.io
  on:
    branch: master
